#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    vector<int> arr1(n);
    vector<int> arr2(n);
    for (int j = 0; j < n;j++){
          cin>>arr1[j];
        }
    for (int j = 0; j < n; j++){
            cin >> arr2[j];
        }
        bool merge = false;
        for (int i = 0; i < n;i++){
            
        }